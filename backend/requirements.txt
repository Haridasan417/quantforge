fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# Data Service (Phase 1)
# pandas-ta 0.4.x requires Python 3.12+ (see CLAUDE.md note on backend Python version)
pandas==2.3.2
numpy==2.2.6
yfinance==1.7.0
nsepy==0.8
pandas-ta==0.4.71b0

# Backtesting (Phase 5)
backtrader==1.9.78.123

# Risk Manager, Trigger & Executor (Phase 6)
redis==8.1.0
smartapi-python==1.5.5
pyotp==2.10.0
logzero==1.7.0
websocket-client==1.9.2

# RL Strategy (Phase 7 part B) -- loads a stable-baselines3 checkpoint
# for live/backtest inference (RLStrategy). Same versions as
# ml/requirements.txt (used for training); stable-baselines3 pinned to
# 2.5.0+, not 2.4.x, since 2.4.x pins numpy<2.0 (see ml/requirements.txt's
# comment) which would conflict with numpy==2.2.6 above.
gymnasium==1.0.0
stable-baselines3==2.5.0
