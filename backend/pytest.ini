[pytest]
pythonpath = .
# Phase 6 added `async def test_...` tests (trigger/executor/risk
# manager touch async DB/Redis code) — auto mode means bare `async def`
# tests run without needing `@pytest.mark.asyncio` on every one.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
